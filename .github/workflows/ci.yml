name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  backend:
    name: Backend lint + test
    runs-on: ubuntu-latest
    # Normally ~5 min; cap a hang well below GitHub's 6-hour default.
    timeout-minutes: 20
    # The tests need a real Postgres (same pinned image as docker-compose.yml; bump both
    # together). The fixtures create and drop their own ansideck_test database in it.
    services:
      postgres:
        image: postgres:18-trixie@sha256:5a5a84b19854a9ffaa54082c166ff4ec27473a361e496e5ea167f298f2da9722
        env:
          POSTGRES_USER: ansideck
          POSTGRES_PASSWORD: ansideck
          POSTGRES_DB: ansideck
          POSTGRES_INITDB_ARGS: --encoding=UTF8 --locale-provider=builtin --builtin-locale=C.UTF-8
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U ansideck -d ansideck"
          --health-interval 2s
          --health-timeout 5s
          --health-retries 30
    env:
      TEST_DATABASE_URL: postgresql+psycopg://ansideck:ansideck@localhost:5432/ansideck_test
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: astral-sh/setup-uv@c18668ad3cf93ea998bef934396af7bb5c839dc7 # v10.2.0
        with:
          version: "latest"
          enable-cache: true
      - run: uv lock --check
      - run: uv sync --all-groups
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - name: Tests with coverage
        run: uv run pytest -v --cov --cov-report=term-missing:skip-covered --cov-report=json:$RUNNER_TEMP/coverage.json

      # The OpenSSF Best Practices claim is about *branch* coverage, and coverage.py's own
      # fail_under only gates the blended line + branch figure, so check branches directly.
      - name: Enforce the branch-coverage floor
        run: uv run python scripts/check_branch_coverage.py "$RUNNER_TEMP/coverage.json" 80

      # Fails on any known vulnerability in the locked runtime + dev dependencies.
      # pip-audit is pinned so the check itself can't change underneath us.
      - name: Dependency audit
        run: |
          uv export --frozen --all-groups --no-hashes --no-emit-project > "$RUNNER_TEMP/requirements.txt"
          uvx pip-audit@2.10.1 -r "$RUNNER_TEMP/requirements.txt" --no-deps --disable-pip

  frontend:
    name: Frontend lint + typecheck + build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - name: Dependency audit
        run: npm audit --audit-level=high
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run build
